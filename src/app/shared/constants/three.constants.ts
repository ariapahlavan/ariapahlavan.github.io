export const FOV = 45; // [degrees] Field of view is the extent of the scene that is seen on the display at any given moment

export const ASPECT_RATIO = window.innerWidth / window.innerHeight; // Width of the element divided by the height

export const NEAR_CLIPPING_PLANE = 1; // Objects closer to the camera than the value of near won't be rendered

export const FAR_CLIPPING_PLANE = 500; // Objects further away from the camera than the value of far won't be rendered

export const RENDERER_WIDTH = window.innerWidth; // [pixel] The width at which our app is rendered

export const RENDERER_HEIGHT = window.innerHeight; // [pixel] The height at which our app is rendered

export const MESH_MATERIAL = {color: 0x00ff00};



